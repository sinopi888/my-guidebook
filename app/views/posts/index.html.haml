= render 'layouts/header'
.postsindex
  .side-bar
    = render 'render/side-bar'
    .container
      .post-list
        - @posts.each do |post|
          %li
            = post.image
            = link_to "#{post.title}", post_path(post.id), class: 'list'